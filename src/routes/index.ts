export enum Tab {
    Habits = "habits",
    Dailies = "dailies",
    Todos = "todos",
    Rewards = "rewards",
}

export { type Task, type ChecklistItem, type Group, type CreateTaskData, type TaskType } from "$lib";
